name: swap_it
description: Erichthonius 2022 Flutter Hackathon entry

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: 0.17.0
  flutter_bloc: 7.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  intl_generator: 0.1.0+0

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/swap_it_logo.png
